pub async fn test_github(context: &str) {
    //let octocrab = octocrab::instance();
    //let issues = octocrab.issues(owner, repo);

    //let issue = match issues.get(13).await {
    //    Ok(issue) => issue,
    //    Err(err) => panic!("Error {:?}", err)
    //};

    //print!("{:#?}", issue);
    print!("context: {:?}", context);
}
